#!/bin/sh
# Remove trailers automáticos do Cursor (roda após a mensagem ser montada)
if [ -f "$1" ]; then
  grep -v -i -E '^Co-authored-by: Cursor|^Made-with: Cursor|cursoragent@cursor\.com' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
